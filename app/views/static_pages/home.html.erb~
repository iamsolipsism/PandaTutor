<% if signed_in? %>
<div class="column"  
<div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
      <section>
        <%= render 'shared/course_form' %>
      </section>
    </aside>
  </div>
<div class="span8">
<%= form_for(@user) do |f| %>
    <% if @user.courses.any? %>
      <h3>Courses (<%= @user.courses.count %>)</h3>
      <ol class="courses">
        <%= render @courses %>
      </ol>
      <%= will_paginate @courses %>
    <% end %>
<% end %>
  </div>
</div>
<% else %>
<div class="center hero-unit">
  <h1>Welcome to PandaTutor!</h1>
<%= link_to image_tag("panda.png", alt: ""), '' %>
  <h2>
    You've taken the class. You've made the grade. Now make bank.
  </h2>

  <%= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary" %>
</div>
<% end %>


<!--<%= link_to image_tag("rails.png", alt: "Rails"), 'http://rubyonrails.org/' %>-->
